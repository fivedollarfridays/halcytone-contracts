---
id: T1.7
title: Drift helpers + top-level exports
plan: plan-sprint-1-engage
type: feature
priority: P1
complexity: 3
status: done
sprint: '1'
depends_on:
- T1.2
- T1.3
- T1.4
- T1.5
completed_at: '2026-04-17T11:55:39.207731'
---

# Drift helpers + top-level exports

Wire the public surface area. Expand `halcytone_contracts/__init__.py` to re-export everything the fleet will import. Add `__contract_version__` (string, must match `pyproject.toml` version) and re-export `SCHEMA_VERSION` from storage as `REQUIRED_SCHEMA_VERSION`. Add two drift helpers: `validate_stream_roster` (verifies a published set covers required streams, raises `ContractError` with a diff on mismatch) and `check_contract_version` (hard-fails on major-version mismatch, warns on minor-version mismatch — per prior decision).

# Acceptance Criteria

- [x] `__contract_version__: str` defined in `__init__.py` and equals the version in `pyproject.toml`
- [x] Test: `__contract_version__` matches `pyproject.toml` version string (parse pyproject at test time)
- [x] `REQUIRED_SCHEMA_VERSION: int` exported from `storage.py` and re-exported at top level (equals `SCHEMA_VERSION`)
- [x] `validate_stream_roster(published: Iterable[str], required: Iterable[str]) -> None` raises `ContractError` with a diff (what's missing) when `published ⊉ required`; returns `None` when `published ⊇ required`
- [x] `check_contract_version(consumer_version: str) -> None`: raises `ContractError` on major-version mismatch; emits `warnings.warn` on minor-version mismatch; no-op on exact or patch-only mismatch
- [x] `ContractError` exception class defined (sensible base — e.g., `RuntimeError` subclass)
- [x] `__init__.py` exports (at minimum): `SignalPacket`, `StreamSpec`, `RESERVED_STREAMS`, `StateVector`, `SessionStart`, `SessionStop`, `Annotation`, `MapperConfigUpdate`, `SessionManifest`, `SCHEMA_VERSION`, `REQUIRED_SCHEMA_VERSION`, `__contract_version__`, `validate_stream_roster`, `check_contract_version`, `ContractError`
- [x] Test: `check_contract_version` on a major-mismatch input raises `ContractError`
- [x] Test: `check_contract_version` on a minor-mismatch input emits a `UserWarning` (asserted via `pytest.warns`)
- [x] Test: `check_contract_version` on a matching version is a no-op (no raise, no warn)
- [x] Test: `validate_stream_roster` passes when `published ⊇ required`
- [x] Test: `validate_stream_roster` raises `ContractError` on missing stream, and the message names the missing stream(s)
- [x] `bpsai-pair arch check` clean on all modified modules