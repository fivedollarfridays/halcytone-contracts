---
id: T2.3
title: SessionManifest rewire + regenerated schema
plan: plan-sprint-2-engage
type: feature
priority: P0
complexity: 5
status: done
sprint: '2'
depends_on:
- T2.1
- T2.2
completed_at: '2026-04-18T00:12:02.467208'
---

# SessionManifest rewire + regenerated schema

Replace `SessionManifest`'s two untyped dicts with the new models from T2.1 + T2.2. `baselines` becomes `Baseline`; `summary` becomes `SessionSummary`. Regenerate the JSON schema mirror (`bundles/manifest.schema.json`) via `scripts/regen_manifest_schema.py` and commit the regenerated file — the CI `schema-drift` job will fail the PR otherwise. Update `tests/test_manifest.py` sample payloads to construct real `Baseline` + `SessionSummary` instances, and update the README manifest example block to reflect the new shape.

# Acceptance Criteria

- [x] `SessionManifest.baselines: Baseline` (was `dict[str, float]`)
- [x] `SessionManifest.summary: SessionSummary` (was `dict[str, float]`)
- [x] `scripts/regen_manifest_schema.py` re-run; `halcytone_contracts/bundles/manifest.schema.json` committed byte-equal to regen output
- [x] `tests/test_manifest.py` sample payloads updated — no remaining `dict[str, float]` literals for baselines/summary
- [x] Round-trip YAML test with a fully populated `Baseline` (multi-stream) and `SessionSummary` (all fields set) passes
- [x] Test: manifest rejects dict-shaped `baselines` or `summary` with a helpful error message (covers the breaking-change migration case)
- [x] README.md manifest example block (around lines 157-168) updated to reflect new shape (real `Baseline` / `SessionSummary` YAML)
- [x] README.md updated links still resolve (no dangling references)
- [x] `ci.yml` `schema-drift` job passes locally: `python scripts/regen_manifest_schema.py && git diff --exit-code halcytone_contracts/bundles/manifest.schema.json`
- [x] `bpsai-pair arch check halcytone_contracts/bundles/manifest.py` clean