---
id: T2.4
title: Top-level exports for Baseline + SessionSummary
plan: plan-sprint-2-engage
type: feature
priority: P0
complexity: 2
status: done
sprint: '2'
depends_on:
- T2.1
- T2.2
completed_at: '2026-04-18T00:24:34.860815'
---

# Top-level exports for Baseline + SessionSummary

Expand `halcytone_contracts/__init__.py` to re-export `Baseline`, `StreamBaseline`, and `SessionSummary` at the top level. Update `tests/test_exports.py`'s `REQUIRED_EXPORTS` tuple + identity-check tests to cover the new surface. Fleet consumers (starting with halcytone-core) should write `from halcytone_contracts import Baseline, SessionSummary` without reaching into submodules.

# Acceptance Criteria

- [x] `__init__.py` imports `Baseline`, `StreamBaseline` from `halcytone_contracts.baseline` and `SessionSummary` from `halcytone_contracts.summary`
- [x] `__init__.py` `__all__` includes `Baseline`, `StreamBaseline`, `SessionSummary` in alphabetical position
- [x] `tests/test_exports.py::REQUIRED_EXPORTS` includes `Baseline`, `StreamBaseline`, `SessionSummary`
- [x] `test_all_declares_required_exports` and `test_every_required_export_is_attribute` both still pass
- [x] New identity-check test: `hc.Baseline is halcytone_contracts.baseline.Baseline`, same for `StreamBaseline` and `SessionSummary`
- [x] `ruff check .` clean — no unused imports, no circular imports
- [x] Fresh-import smoke: `python -c "from halcytone_contracts import Baseline, StreamBaseline, SessionSummary"` succeeds