---
id: T1.6
title: bundles/manifest.py + generated manifest.schema.json
plan: plan-sprint-1-engage
type: feature
priority: P1
complexity: 6
status: done
sprint: '1'
depends_on:
- T1.4
completed_at: '2026-04-17T11:51:19.060549'
---

# bundles/manifest.py + generated manifest.schema.json

Build the session-bundle manifest — the cross-language boundary artifact consumed by the TypeScript AgentGrounds side. `SessionManifest` pydantic v2 model covers every field from the README manifest. A regen script emits `manifest.schema.json` via `model_json_schema()`, and that generated file is checked in. A byte-equality test compares fresh regen output against the checked-in file, giving CI a drift detector for free.

# Acceptance Criteria

- [x] `halcytone_contracts/bundles/__init__.py` exists
- [x] `SessionManifest` pydantic v2 model with fields: `session_id`, `started_at`, `ended_at`, `duration_s`, `sensors`, `baselines`, `summary`, `artifacts` (matching README)
- [x] `session_id` field validated via T1.4's regex (invalid IDs raise `ValidationError`)
- [x] `ended_at` is `Optional[datetime]`; presence marks session completion (matches README semantic)
- [x] `scripts/regen_manifest_schema.py` emits `halcytone_contracts/bundles/manifest.schema.json` via `SessionManifest.model_json_schema()`
- [x] `manifest.schema.json` checked into the repo
- [x] Test: round-trip yaml → `SessionManifest` → yaml preserves all structural fields
- [x] Test: running `scripts/regen_manifest_schema.py` produces output byte-equal to the checked-in `manifest.schema.json` (CI drift check)
- [x] Test: `ended_at=None` accepted (in-progress session); populated `ended_at` accepted (completed session)
- [x] `bpsai-pair arch check halcytone_contracts/bundles/manifest.py` clean