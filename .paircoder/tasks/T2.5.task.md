---
id: T2.5
title: Bump to v0.2.0 + CHANGELOG + ROADMAP
plan: plan-sprint-2-engage
type: feature
priority: P0
complexity: 2
status: done
sprint: '2'
depends_on:
- T2.3
- T2.4
completed_at: '2026-04-18T00:26:22.226349'
---

# Bump to v0.2.0 + CHANGELOG + ROADMAP

Cut the v0.2.0 release. Update `pyproject.toml` version and `__contract_version__` in lockstep (the `test_matches_pyproject` test enforces this). Seed a `[0.2.0]` section in `CHANGELOG.md` under Keep-a-Changelog format — include a `### Changed` subsection documenting the breaking field-type change on `SessionManifest.baselines` / `SessionManifest.summary`, and an `### Added` subsection listing the three new models. Promote v0.2.0 items from "next minor" to "shipped" in `ROADMAP.md`; promote any v0.3.0 items that are now imminent. Include an explicit migration note for consumers.

# Acceptance Criteria

- [x] `pyproject.toml` `version = "0.2.0"`
- [x] `halcytone_contracts/__init__.py` `__contract_version__: str = "0.2.0"`
- [x] `tests/test_exports.py::test_matches_pyproject` passes
- [x] `CHANGELOG.md` has a `## [0.2.0] — 2026-04-17` section (or today's date), with:
- [x] `### Changed` listing the breaking `SessionManifest.baselines` / `SessionManifest.summary` field-type change
- [x] `### Added` listing `Baseline`, `StreamBaseline`, `SessionSummary`
- [x] Explicit migration note: "Breaking: consumers pinned to `>=0.1,<0.2` must re-pin to `>=0.2,<0.3`; `check_contract_version("0.1.x")` now hard-fails per the 0.x sharpened policy"
- [x] CHANGELOG comparison links updated: `[Unreleased]`, `[0.2.0]`, `[0.1.1]`, `[0.1.0]`
- [x] `ROADMAP.md`: v0.2.0 items moved from "next minor" to "shipped"; v0.3.0 items tightened based on what remains
- [x] Full test suite green; `ruff check .` clean; schema drift check clean
- [x] `bpsai-pair arch check .` reports no new violations