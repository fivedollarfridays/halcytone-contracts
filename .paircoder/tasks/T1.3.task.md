---
id: T1.3
title: state.py (StateVector)
plan: plan-sprint-1-engage
type: feature
priority: P0
complexity: 4
status: done
sprint: '1'
depends_on:
- T1.1
completed_at: '2026-04-17T11:39:11.743955'
---

# state.py (StateVector)

Author the fused `StateVector` pydantic v2 model — the per-tick output of the downstream fusion layer, logged one-per-line to `state.jsonl`. Flat struct by design (no nested models), matching the README's "flat struct by choice" note. Normalize numeric ranges via pydantic validators so downstream consumers can trust bounds without re-checking.

# Acceptance Criteria

- [x] Every field from the README's `StateVector` spec present with the correct type
- [x] `breath_phase` validated in `[0, 1]`
- [x] `breath_depth` validated in `[0, 1]`
- [x] `heart_breath_coherence` validated in `[0, 1]`
- [x] `overall_presence` validated in `[0, 1]`
- [x] All `eeg_*` normalized fields validated in `[0, 1]`
- [x] Model is a flat struct — no nested pydantic sub-models
- [x] Test: round-trip JSON serialize/deserialize preserves all fields
- [x] Test: JSONL-line serialization — `model_dump_json()` output fits one line and parses back via `StateVector.model_validate_json()`
- [x] `bpsai-pair arch check halcytone_contracts/state.py` clean